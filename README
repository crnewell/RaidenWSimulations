installation instructions:

create virtual environment (if neccessary):
python -m venv ./venv

activate virtual environment, and run
pip install -r requirements.txt

Then activate the virtual environment with
./venv/Scripts/activate 

Then run
python main.py
